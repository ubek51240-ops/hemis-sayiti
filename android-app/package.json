{
  "name": "talaba-qabul-mobile",
  "version": "1.0.0",
  "description": "Test-sinov-ucun - Android mobile app (Capacitor)",
  "private": true,
  "scripts": {
    "sync": "cap sync",
    "open:android": "cap open android",
    "build:android": "cap sync && cd android && ./gradlew assembleDebug",
    "build:release": "cap sync && cd android && ./gradlew assembleRelease"
  },
  "dependencies": {
    "@capacitor/android": "^6.1.2",
    "@capacitor/app": "^6.0.1",
    "@capacitor/core": "^6.1.2",
    "@capacitor/network": "^6.0.3",
    "@capacitor/splash-screen": "^6.0.2",
    "@capacitor/status-bar": "^6.0.1",
    "@capacitor/toast": "^6.0.2"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.1.2"
  }
}
